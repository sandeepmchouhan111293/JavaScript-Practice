/*Const is used to create coonstants whose value will not change.
 *If we try to change it will give error.
 */
const prefix = "Mr.";
console.log(prefix);

/*
 *  let is used to create variables in ES6 javascript it's new and used now a days.
 */
let firstName = "Sandeep";
console.log(firstName);

/*
 *  var is used to create variables in javascript it's old and not used now a days.
 */
var lastName = "Chouhan";
console.log(lastName);

console.log(prefix + " " + firstName + " " + lastName);
